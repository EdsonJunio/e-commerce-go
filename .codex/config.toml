# GitHub's official remote MCP server.
# Read the token at connection time without storing it in this config.
[mcp_servers.github]
url = "https://api.githubcopilot.com/mcp/x/all"
http_headers_helper = "/bin/sh /home/desk/Projects/e-commerce-go/.codex/github-mcp-headers.sh"
enabled = true
required = false
startup_timeout_sec = 20
tool_timeout_sec = 60
default_tools_approval_mode = "writes"
